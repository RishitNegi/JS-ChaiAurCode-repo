"use strict"; // When you write this, the compiler reads all JS code as "Newer" version

// alert(3 + 3); //We are using nodejs, not browser, so alert wouldn't work.

console.log(3 
    + 
    3); //code being functional doesn't equal code being readable. Try to write conventional codes that are readable under good coding practice.

console.log("Rishit");

let name = "Rishit" //String => ""
let age = 18  // Number => 2 to the power 53 is the upper limit
let isLoggedIn = false  //Boolean => true/false
// null => standalone value 
// undefined => 

